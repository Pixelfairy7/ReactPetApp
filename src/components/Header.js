import React from 'react';
import '../App.scss';

function Header() {
  return (
    <header className="header">
        <h1>Find your furever friend!</h1>
        <h2>Search for adoptable pets in your area</h2>
    </header>
    
  );
}

export default Header;
